# 数据源开关
# 设为 false 时使用前端 mock 数据，不依赖后端服务
# 设为 true 或不设置时使用后端 API（需启动 my_server）
# 注意：修改后需重启 dev server (npm run dev)
VITE_USE_BACKEND=false

# 运行时覆盖（无需重启）：
# - 访问 ?mock=1 强制使用 mock
# - 控制台执行 localStorage.setItem('VITE_USE_MOCK','1') 后刷新
